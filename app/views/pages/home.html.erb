<video width="100%" height="100%" playsinline autoplay muted loop>
  <source src="<%= asset_path('final-video-cropped.mp4') %>" type="video/mp4">
  <source src="movie.ogg" type="video/ogg">
</video>
  <div class="my-5 text-center">
    <h1 class="form-title">NEW RELEASES</h1>
  </div>
  <div class="container">
    <div class="cards">
      <% @products.sample(3).each do |prod| %>
        <div class="card-trip">
          <div class="card-link">
            <%= link_to product_path(prod) do %>
              <div class="img" style="background-image: url(<%= cl_image_path prod.pictures.first.url%>)">
              </div>
            <% end %>
          </div>
          <div class="card-trip-infos mb-5">
            <h5><strong><%= prod.name %></strong></h5>
            <p><%= prod.price %>€</p>
            <p><%= prod.colour.name %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>

